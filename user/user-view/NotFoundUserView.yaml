description: Not found user
type: object
properties:
  id:
    type: string
    example: disabled
    readOnly: true
  firstName:
    type: string
    enum: [Previous]
    readOnly: true

  lastName:
    type: string
    enum: [Employee]
    readOnly: true

  avatar:
    type: string
    nullable: true
    enum: [null]
    readOnly: true

  department:
    type: string
    enum: [-]
    readOnly: true

  position:
    type: string
    enum: [-]
    readOnly: true

required:
  - id
  - firstName
  - lastName
  - avatar
  - department
  - position
