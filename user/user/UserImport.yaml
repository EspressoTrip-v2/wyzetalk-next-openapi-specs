type: object
properties:
  strategy:
    type: string
    description: The strategy to use when bulk importing users
    enum: ["SKIP","OVERWRITE"]
    example: OVERWRITE
  users:
    type: array
    items:
      type: object
      $ref: "./UserImportItem.yaml"
required:
    - strategy
    - users