description: Post content form data
type: object
properties:
  type:
    type: string
    enum: [video]
    example: image
  url:
    type: string
    example: "https://some-external.com"
  formType:
    type: string
    example: "poll"
required:
  - type
  - url
  - formType