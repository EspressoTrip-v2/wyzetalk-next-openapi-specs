description: Post content video data
type: object
properties:
  type:
    type: string
    enum: [video]
    example: image
  url:
    type: string
    example: "https://some-external.com"
required:
  - type
  - url